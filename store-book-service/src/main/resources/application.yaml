spring:
  cloud:
    stream:
      kafka:
        binder:
          auto-add-partitions: true
      bindings:
        bookProcessing:
          destination: check-book
          group: group
          binder: kafka